import styled from 'styled-components';
import { center } from '../../styles/tools';

const Description = styled.p `
  font-size: 18px;
  margin-bottom: var(--spacing-big);
  max-width: 270px;
  ${center};
  min-height: var(--spacing-big);
`

export default Description;
